# OctoPunk 产品路线图

> 本文记录 OctoPunk 在当前 MVP 基础上的产品发展方向、技术优化重点与阶段性优先级。
> 路线图强调可靠编排、质量审查和安全治理，不以单纯增加 Agent 数量、终端面板或视觉主题作为核心竞争力。

## 1. 产品定位

OctoPunk 的目标不是成为普通的多 Agent 会话管理器，而是成为：

> 面向主 Agent 的本地、安全、可恢复、可审计的 Coding Agent 执行控制平面。

核心价值包括：

- Codex 通过 MCP 充当编排者和最终审查者；
- Claude Code、Codex 等外部 Agent 在受控环境中执行任务；
- 通过 Git worktree、分支和执行模式隔离只读任务与写入任务；
- 通过 DAG、持久化队列和依赖报告组织复杂任务；
- 通过 `PASS`、`REWORK`、`BLOCKED` 形成可恢复的审查闭环；
- 通过 SQLite、幂等请求、attempt、report 和事件记录保留完整审计证据。

## 2. 当前基础与主要边界

当前版本已经具备：

- Claude Code / Codex 混合委派；
- `read_only` / `workspace_write` 两种执行模式；
- 原子批量委派、父子关系、依赖 DAG 和持久化队列；
- 独立 worktree、分支集成和目标分支安全检查；
- MCP STDIO 控制面与 HTTP 兼容入口；
- 任务事件、执行日志、报告和审查状态持久化；
- 取消、恢复、返工、阻塞、丢弃和归档；
- 可配置的单次 TeamRun 子 Agent 并发限制。

当前主要边界：

- 同一时间只能有一个活跃 TeamRun；
- 主要支持 Claude Code 和 Codex；
- 审查仍以报告和日志为主，缺少完整 Diff/CI/PR 工作台；
- 失败诊断和恢复仍需要用户理解 CLI、PATH、登录、Git 与 MCP 细节；
- 缺少稳定的 headless 自动化入口、预算治理和外部系统集成；
- 目前以 macOS 本地单用户场景为主。

## 3. 发展原则

1. **可靠性优先于功能数量**：任务必须可恢复、可解释、可审计。
2. **安全默认**：最小权限、明确隔离、显式集成、危险操作可阻断。
3. **证据驱动审查**：PASS 必须能够关联 Diff、测试和审查发现。
4. **Agent 可驱动**：GUI、MCP、CLI/API 应共享同一套领域状态机。
5. **人始终可接管**：自动化不能取消用户审查、暂停、返工和终止能力。
6. **插件化扩展**：新增 Agent、触发器和集成不应持续侵入核心调度逻辑。
7. **先本地稳定，再扩展团队和云能力**。

## 4. v0.3：稳定性与多任务运行

目标：从功能完整的 MVP 发展为可以长期依赖的本地控制平面。

### 4.1 多项目、多 TeamRun

- 支持多个仓库同时存在活跃 TeamRun；
- 支持同一仓库多个 TeamRun，但对目标分支集成进行串行化；
- 提供全局并发、单项目并发和单 Provider 并发限制；
- 支持 TeamRun 优先级、暂停、继续和排队调整；
- 增加全局工作台，汇总：
  - 正在运行；
  - 排队等待；
  - 等待用户输入；
  - 执行失败；
  - 等待审查；
  - 可以集成。

### 4.2 环境诊断中心（Doctor）

在 TeamRun 启动前主动检查：

- Claude、Codex 可执行文件路径与版本；
- GUI 进程实际继承的 PATH；
- CLI 登录和会话可用状态；
- MCP STDIO 初始化和工具发现；
- Git 仓库、HEAD、分支和 dirty 状态；
- worktree 创建、删除和磁盘空间；
- 沙箱能力与必要系统权限；
- Provider 限流、配额和并发额度；
- 数据库健康状态和迁移版本。

诊断结果应包含：

- 可读的失败原因；
- 影响范围；
- 推荐处理方式；
- 可复制的脱敏诊断包；
- 修复后单项重新检测。

### 4.3 故障分类与恢复

- 将错误划分为临时错误、配置错误、任务错误、代码错误和系统错误；
- 临时错误允许在明确策略下有限次数指数退避；
- 显示“是否安全重试”和重试依据；
- App 重启后恢复活跃 TeamRun 状态并重新核对存活进程；
- 检测孤儿进程、孤儿 worktree、临时分支和残留锁；
- 支持从失败节点或指定 DAG 节点重新运行；
- 保留每次恢复和重试的 attempt 与事件证据；
- 禁止静默无限重试。

### 4.4 资源感知调度

- 综合 CPU、内存、磁盘和 Provider 并发额度调节任务启动速度；
- 支持按 Agent 类型限制并发；
- 为交互型任务预留执行槽；
- 展示任务排队原因和预计启动顺序；
- 在系统压力过高时暂停启动新任务，而不破坏运行中的任务。

### v0.3 验收目标

- 多个仓库能够同时运行且互不污染；
- App 异常退出后可以恢复并解释所有未完成任务的状态；
- 常见 PATH、登录、MCP、Git 和磁盘问题能够在委派前发现；
- 用户无需打开终端即可完成大部分故障诊断和恢复。

## 5. v0.4：Review Center 与质量门禁

目标：把“Agent 完成了任务”升级为“结果具有足够证据，可以安全交付”。

### 5.1 Review Center

- 文件变更树和完整 Diff；
- 行级评论和多评论批量返工；
- 基线、当前工作树和集成结果对比；
- 报告、执行日志、Diff 和测试证据联动；
- 高风险文件、敏感文件、超大 Diff 和越界修改提示；
- unresolved findings 列表；
- 合并冲突预览和处理流程；
- PR 创建、CI 状态、Review comment 和检查结果回灌；
- 审查结束时生成结构化交付摘要。

### 5.2 自动质量门禁

支持为项目或 Workflow 配置 PASS 条件，例如：

- 必要测试、lint、typecheck、build 通过；
- 不存在未处理的高风险发现；
- 修改范围未超出允许目录；
- 所有依赖任务已经接受；
- 目标分支仍处于安全基线；
- 必要 Reviewer 已通过；
- 数据库迁移、安全配置等高风险变更已经人工确认；
- 已知 Todo、评论和阻塞项全部清零。

质量门禁应输出明确的通过项、失败项和豁免记录，不能只返回布尔值。

### 5.3 跨模型审查与仲裁

支持以下内置模式：

- Claude 实现，Codex 审查；
- Codex 实现，Claude 审查；
- 两个只读 Agent 独立调查，主 Agent 综合；
- 多个 Agent 并行生成方案，再由测试和 Reviewer 选择；
- 安全、架构、测试等不同角色分别审查；
- 高风险任务采用多 Reviewer 仲裁；
- 出现结论冲突时输出共识、分歧和待验证项。

### v0.4 验收目标

- 用户可以在 OctoPunk 内完成从 Diff 审查到返工再到最终集成的完整流程；
- 每次 PASS 都能追溯到测试、审查和 Git 状态证据；
- 质量门禁失败时能够准确说明原因并把修复意见发送回原会话。

## 6. v0.5：可视化工作流与上下文治理

目标：从任务调度器升级为可复用的 Agent 工作流执行器。

### 6.1 可视化、可编辑的 DAG

- 拖拽建立任务依赖和父子关系；
- 展示关键路径、可并行节点和阻塞原因；
- 支持 fan-out / fan-in；
- 运行中新增、取消或调整尚未启动的节点；
- 节点输出可声明为后续任务输入；
- 失败后只重新执行受影响的下游节点；
- 根据文件范围和历史数据预测修改冲突；
- 保存 DAG 为可复用 Workflow。

### 6.2 Workflow 模板

提供内置模板：

- 只读代码审计；
- Bug 调查 → 修复 → 测试 → Review；
- 前端 / 后端 / 测试并行开发；
- 多方案竞赛；
- 依赖升级；
- 安全审计；
- PR Review；
- 文档与实现一致性检查。

模板可以定义：

- Agent 类型、模型和角色；
- `read_only` / `workspace_write`；
- DAG 和并发限制；
- 工具、目录、网络和写入权限；
- 质量门禁；
- 最大审查轮次、执行时间和预算；
- 最终报告格式。

### 6.3 上下文供应与溯源

- 自动生成任务最小上下文包；
- 将文件、符号、历史决策和依赖报告分层提供；
- 支持 Agent 按需拉取新上下文；
- 标记上下文来源、生成时间和版本；
- 检测上下文是否因代码变化而过期；
- 相同调查结果可以在同一 TeamRun 内复用；
- 报告可以强制包含文件、行号、命令和测试证据；
- 对注入内容继续执行大小限制、脱敏和审计。

### v0.5 验收目标

- 用户可以不编写 MCP 调用参数，通过可视化方式创建复杂任务图；
- 常用工作流能够保存、复用和导出；
- 失败节点局部重跑不会重复执行无关任务；
- 每个重要结论都可以追溯到来源和适用的代码版本。

## 7. v0.6：自动化、集成与成本治理

目标：支持无人值守运行，并与现有工程流程连接。

### 7.1 Headless 与自动触发

- 提供稳定的本地 CLI 或 HTTP API；
- GUI、MCP、CLI/API 共享同一应用服务和权限模型；
- 支持定时任务；
- 支持 Git hook、文件变化和 webhook 触发；
- 支持 GitHub Issue、PR、CI 失败触发 TeamRun；
- 支持完成、阻塞、超预算和等待输入通知；
- 支持系统通知、Webhook、Slack 等可选出口；
- 提供查询运行、任务、报告和审计记录的只读接口。

### 7.2 用量、预算与模型路由

- 按 TeamRun、任务、Agent、Provider 和模型统计 token/用量；
- 支持每日、每月和单任务预算；
- 支持最大执行时间和最大审查轮次；
- 达到阈值后暂停、请求确认或降级模型；
- 展示并发限流和套餐额度对吞吐的影响；
- 统计返工次数、通过率、任务耗时和最终采用率；
- 基于历史数据辅助选择适合任务类型的 Agent/模型；
- 所有成本数据标明来源和估算误差。

### 7.3 Provider 插件体系

先定义稳定的 Provider 协议，再扩展更多 Agent。协议至少包括：

- 可用性和能力声明；
- 启动、恢复、取消和终止；
- 权限请求与审批；
- 流式事件和结构化报告；
- session/thread ID；
- token 与费用信息；
- MCP 注入和受限上下文获取；
- 错误分类和安全重试语义。

候选 Provider：

- Gemini CLI；
- OpenCode；
- GitHub Copilot CLI；
- Cursor 或其他本地 Agent；
- 用户自定义 CLI Adapter。

### v0.6 验收目标

- OctoPunk 可以在没有 GUI 窗口参与的情况下安全完成预定义 Workflow；
- 外部系统能够触发任务并查询结果；
- 用户可以设置预算并知道资源消耗在什么任务上；
- 新 Provider 可以通过稳定 Adapter 接口接入，而不是修改核心状态机。

## 8. v1.0：团队与企业能力

在本地单用户体验稳定后，再考虑：

- 团队共享 Workflow 模板；
- 项目级角色、权限和 Agent 白名单；
- 管理员强制质量门禁；
- 组织级模型和成本策略；
- 审计记录导出、签名与长期留档；
- 企业环境禁网、代理和私有 Provider；
- 私有插件源；
- 团队运行看板和只读观察者；
- 可选的加密同步与远程监督。

团队协作和云同步会引入账号、权限、加密、冲突、合规和运维成本，不应早于本地控制平面的稳定性建设。

## 9. 持续技术优化

以下工作不绑定单一版本，应持续推进。

### 9.1 Provider 与协议测试

- 为每个 Provider 建立统一 contract tests；
- 对真实 Claude/Codex CLI 做最小 E2E，而不仅依赖 mock；
- 验证启动、输出、报告、恢复、取消、权限请求和异常退出；
- 对 MCP 请求、通知、幂等和超时做协议回归测试。

### 9.2 故障注入

覆盖以下场景：

- 强制退出 App；
- 杀死子 Agent 进程；
- 网络中断和 Provider 529；
- 登录失效；
- JSONL/JSON-RPC 数据损坏；
- 磁盘空间不足；
- 数据库锁定或迁移失败；
- 目标分支移动或工作区变脏；
- worktree、临时分支和进程残留。

### 9.3 数据与性能

- SQLite 迁移验证、备份和修复；
- 事件流背压、聚合、压缩和保留策略；
- 大型日志、超大 Diff、数万事件下的性能测试；
- 热数据与历史数据分层加载；
- worktree 磁盘统计、安全回收和清理预估；
- 避免 UI 为非可见运行持续订阅全量事件。

### 9.4 安全与可解释性

- 目录、命令、网络和工具权限采用最小授权；
- 明确显示权限请求由谁发起、为何需要、影响范围是什么；
- 记录策略命中和拒绝原因；
- 日志、上下文、报告和诊断包统一脱敏；
- 对高风险操作保留人工确认点；
- MCP tracing 可追踪但不得泄露密钥和完整敏感内容。

### 9.5 可观测性与复现

- 为 TeamRun、任务、attempt 和进程建立统一 trace ID；
- 展示调度等待、模型执行、工具调用、测试和审查耗时；
- 支持导出脱敏的可复现诊断包；
- 支持将事件时间线重放为运行报告；
- 将基线失败与本次改动引入的失败分开呈现。

## 10. 暂不优先的方向

在核心可靠性、审查和恢复能力完成前，以下方向不应占用主要研发资源：

- 仅为了数量增加大量 Agent Provider；
- 复杂终端模拟器和 IDE 全量替代；
- 大量主题、动画和非关键视觉定制；
- 没有明确隔离模型的远程任意命令执行；
- 过早建设团队云同步和账号体系；
- 静默自动合并、自动批准或无限重试；
- 只展示“Agent 已完成”，却没有 Diff、测试和证据的成功状态。

## 11. 推荐实施顺序

如果资源有限，优先完成以下三项：

1. **多项目、多 TeamRun**：解除当前最明显的吞吐和使用场景限制；
2. **Review Center + 质量门禁**：让结果真正具备可交付性；
3. **Doctor + 故障恢复**：降低本地 CLI、PATH、登录、MCP、Git 和进程问题造成的使用门槛。

完整版本顺序：

1. `v0.3`：稳定性、多 TeamRun、Doctor、恢复和资源调度；
2. `v0.4`：Diff、测试证据、Review Center、质量门禁和跨模型审查；
3. `v0.5`：可视化 DAG、Workflow 模板、局部重跑和上下文治理；
4. `v0.6`：Headless API、外部集成、预算治理和 Provider 插件；
5. `v1.0`：团队策略、审计导出和企业能力。

## 12. 路线图成功指标

路线图不应只按功能是否上线衡量，还应关注：

- TeamRun 成功恢复率；
- 环境问题在任务启动前的发现率；
- BLOCKED 原因可诊断率；
- 无需人工终端操作即可恢复的失败比例；
- 单任务平均返工轮次；
- 质量门禁拦截的真实问题数量；
- PASS 后出现回滚或人工补救的比例；
- 多项目并发时的稳定性和资源利用率；
- 上下文复用带来的重复扫描和 token 降低比例；
- 不同 Agent/模型在各类任务上的质量、耗时与成本表现。

最终目标是让用户能够放心地把复杂任务交给多个 Agent，同时始终知道：谁在做什么、使用了什么权限、产生了哪些变更、为什么可以通过，以及失败后如何恢复。
